<h1>Appointment Info</h1>

<table>
  <thead>
    <tr>
      <th>Date</th>
      <th>Patient Name</th>
      <th>Doctor Name</th>
    </tr>
  </thead>
  <tbody>
    <tr>
        <td><%= @appointment.appointment_datetime.strftime("%A, %B %d, %Y at %T") %></td>
        <td><%= @appointment.patient.name, appointment_path(@appointment) %></td>
        <td><%= link_to @appointment.doctor.name, appointments_path(@appointment) %></td>
    </tr>
  </tbody>
</table>
